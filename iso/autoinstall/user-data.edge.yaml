#cloud-config
# Starship OS autoinstall — edge profile
autoinstall:
  version: 1
  locale: en_US.UTF-8
  keyboard:
    layout: us
  identity:
    hostname: starship-edge
    username: starship
    password: "REPLACE_WITH_MKPASSWD_HASH"
  ssh:
    install-server: true
    allow-pw: true
  storage:
    layout:
      name: direct
  packages:
    - python3
    - python3-venv
    - curl
    - git
    - ca-certificates
  late-commands:
    - curtin in-target --target=/target -- bash -c 'mkdir -p /opt/starship/bin /etc/starship /var/lib/starship /var/log/starship && ln -sfn /opt/starship /opt/agnetic && echo "STARSHIP_PROFILE=edge" > /etc/starship/firstboot.env && echo "profile: edge" > /etc/starship/profile.yaml'
    - curtin in-target --target=/target -- bash -c 'if [ -x /opt/starship/bin/starship-firstboot.sh ]; then STARSHIP_PROFILE=edge /opt/starship/bin/starship-firstboot.sh; elif [ -f /opt/starship/lib/starship/scripts/starship-firstboot.sh ]; then STARSHIP_PROFILE=edge bash /opt/starship/lib/starship/scripts/starship-firstboot.sh; fi || true'
