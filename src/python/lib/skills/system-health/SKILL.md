# System Health

Monitors and maintains system resources, processes, and logs.

## Capabilities
- resource_monitoring (CPU, memory, disk, network)
- process_management (pgrep, pkill, systemctl status)
- log_analysis (journalctl, tail, grep)
- alerting (threshold-based notifications)
- maintenance (cleanup, cron, updates)
